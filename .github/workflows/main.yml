name: RDP-ngrok

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Enable and Configure RDP
        run: |
          # Enable RDP
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0 -Force
          
          # Disable NLA (important)
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "SecurityLayer" -Value 0 -Force

          # Firewall rules
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
          netsh advfirewall firewall delete rule name="Allow RDP" 2>$null
          netsh advfirewall firewall add rule name="Allow RDP" dir=in action=allow protocol=TCP localport=3389

          # Restart RDP service
          Restart-Service TermService -Force
          Start-Sleep -Seconds 8

          # Check if port 3389 is listening
          $listening = netstat -an | Select-String ":3389"
          Write-Host "Port 3389 status:"
          Write-Host $listening

      - name: Set runneradmin Password
        run: |
          $password = "Pass@" + (Get-Random -Minimum 100000 -Maximum 999999)
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          Set-LocalUser -Name "runneradmin" -Password $securePass

          "RDP_PASSWORD=$password" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host "Password set successfully"

      - name: Install ngrok
        run: |
          Invoke-WebRequest -Uri "https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip" -OutFile "$env:TEMP\ngrok.zip"
          Expand-Archive "$env:TEMP\ngrok.zip" -DestinationPath "$env:TEMP" -Force

      - name: Start ngrok + Show Credentials
        run: |
          & "$env:TEMP\ngrok.exe" config add-authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

          # Start ngrok in background
          Start-Process -FilePath "$env:TEMP\ngrok.exe" -ArgumentList "tcp","3389" -WindowStyle Hidden

          # Wait for tunnel
          Start-Sleep -Seconds 20

          try {
              $tunnels = Invoke-RestMethod -Uri "http://127.0.0.1:4040/api/tunnels"
              $addr = $tunnels.tunnels[0].public_url -replace "tcp://",""
          } catch {
              Write-Error "ngrok tunnel not ready"
              exit 1
          }

          Write-Host ""
          Write-Host "=============================================="
          Write-Host "              RDP DETAILS"
          Write-Host "=============================================="
          Write-Host "Address   : $addr"
          Write-Host "Username  : runneradminabdul"
          Write-Host "Password  : $env:RDP_PASSWORD"
          Write-Host "=============================================="
          Write-Host ""

      - name: Keep Alive
        run: |
          Write-Host "RDP is running... Do not cancel the workflow."
          while ($true) {
              Write-Host "[$(Get-Date -Format 'HH:mm:ss')] Active"
              Start-Sleep -Seconds 300
          }
